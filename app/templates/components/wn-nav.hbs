<nav>
  <div class="nav-wrapper">
    <div class="container">
      {{#if title}}
        {{link-to title 'home' class='brand-logo'}}
      {{/if}}

      <a class="button-collapse">
        <i class="material-icons">menu</i>
      </a>

      {{yield (hash
        desktop-menu=(component 'wn-div'
                        tagName='ul'
                        classNames="right hide-on-med-and-down")
        side-nav=(component 'wn-div'
                    tagName='ul'
                    _parentComponent=this
                    classNames="side-nav")
        link-to=(component 'nav-link')
      )}}
    </div>
  </div>
</nav>
